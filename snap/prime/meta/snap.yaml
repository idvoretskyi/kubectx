name: kubectx
version: 0.5.0
summary: A tool to switch between Kubernetes contexts
description: 'Fast way to switch between clusters and namespaces in kubectl!

  '
architectures:
- amd64
confinement: devmode
grade: devel
apps:
  kubectx:
    command: command-kubectx.wrapper
  kubens:
    command: command-kubens.wrapper
